{
  "name": "@gridora/policy-schedule",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/policy-schedule/test"
  },
  "dependencies": {
    "@gridora/domain": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@gridora/policy-control": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
