{
  "name": "@gridora/game-desired-state-control",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/game-desired-state-control/test"
  },
  "dependencies": {
    "@gridora/plugin-registry": "workspace:*",
    "@gridora/plugin-sdk-control": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@gridora/migrations": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
